extends layout
block content
  a(href='/rss') RSS feed

  - var posts = getPosts(0,5)
  - var page  = 1
  - each post in posts
      include includes/postSnippet

  h1 Cats
  - var tags = getCategories()
  - each tag in tags
    - var url = categoryURL(tag)
    a(href=url)
        = tag
    p


  include includes/pagination